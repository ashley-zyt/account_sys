<% content_for :page_title do %>
  关键词详情
<% end %>
<% content_for :page_subtitle do %>
  <%= @huasheng_keyword.keyword %>
<% end %>

<div class="admin-card">
  <div class="admin-card-header">
    <div>
      <div class="admin-card-title">
        <%= @huasheng_keyword.keyword %>
      </div>
      <div class="admin-card-subtitle">
        主题：<%= @huasheng_keyword.theme %>
      </div>
    </div>
    <div class="admin-card-header-right" style="display: flex; gap: 8px;">
      <%= link_to "返回列表", admin_huasheng_keywords_path, class: "admin-button-secondary" %>
    </div>
  </div>

  <!-- 基本信息 -->
  <div style="padding: 20px 0; border-bottom: 1px solid #334155;">
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px 40px;">
      <div class="info-item">
        <span class="info-label">主题</span>
        <span class="info-value"><%= @huasheng_keyword.theme %></span>
      </div>
      <div class="info-item">
        <span class="info-label">关键词</span>
        <span class="info-value"><%= @huasheng_keyword.keyword %></span>
      </div>
      <div class="info-item">
        <span class="info-label">任务状态</span>
        <span class="info-value"><%= status_badge(@huasheng_keyword.status) %></span>
      </div>
      <% if @huasheng_keyword.task_id.present? %>
      <div class="info-item">
        <span class="info-label">远程任务ID</span>
        <span class="info-value" style="font-family: monospace; font-size: 12px;"><%= @huasheng_keyword.task_id %></span>
      </div>
      <% end %>
      <div class="info-item">
        <span class="info-label">创建时间</span>
        <span class="info-value"><%= @huasheng_keyword.created_at&.strftime('%Y-%m-%d %H:%M:%S') %></span>
      </div>
      <div class="info-item">
        <span class="info-label">更新时间</span>
        <span class="info-value"><%= @huasheng_keyword.updated_at&.strftime('%Y-%m-%d %H:%M:%S') %></span>
      </div>
    </div>
  </div>

  <!-- 原始响应数据 -->
  <%
    result = JSON.parse(@huasheng_keyword.result_data) rescue {}
    object_key = result["oss_url"].to_s.strip.gsub(/^`|`$/, "").strip
    video_url = @video_url
  %>
  <% if @huasheng_keyword.result_data.present? %>
  <div style="padding: 20px 0;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <span style="font-size: 14px; color: #e2e8f0; font-weight: 600;">API 原始响应</span>
      <button id="toggle_raw_btn" style="font-size: 11px; color: #64748b; background: transparent; border: 1px solid #334155; border-radius: 4px; padding: 3px 10px; cursor: pointer;">展开</button>
    </div>

    <% if video_url.present? %>
    <!-- OSS 视频预览（V1 签名 URL，1 年有效期） -->
    <div style="margin-bottom: 12px; background: rgba(15,23,42,0.4); border: 1px solid #334155; border-radius: 6px; padding: 12px;">
      <div style="font-size: 12px; color: #94a3b8; margin-bottom: 8px; display: flex; justify-content: space-between; align-items: center;">
        <span>OSS 视频</span>
        <div style="display: flex; gap: 8px;">
          <a href="<%= video_url %>" target="_blank" style="font-size: 11px; color: #60a5fa; text-decoration: none;">新窗口打开</a>
          <button class="copy-link-btn" data-url="<%= video_url %>" style="font-size: 11px; color: #60a5fa; background: rgba(59,130,246,0.12); border: 1px solid rgba(59,130,246,0.25); border-radius: 4px; padding: 3px 10px; cursor: pointer;" title="复制链接">复制链接</button>
        </div>
      </div>
      <video src="<%= video_url %>" controls preload="metadata"
             style="width: 100%; max-height: 480px; border-radius: 8px; background: #000;"></video>
      <div style="font-size: 11px; color: #64748b; margin-top: 6px;">如画面全黑有声音，请点击"新窗口打开"下载后播放</div>
      <% if object_key.present? %>
      <div style="font-size: 11px; color: #64748b; margin-top: 4px; font-family: monospace;">object_key: <%= object_key %></div>
      <% end %>
    </div>
    <% end %>

    <pre id="raw_data_block" style="display: none; background: rgba(15,23,42,0.6); border: 1px solid #334155; border-radius: 6px; padding: 12px; color: #cbd5e1; font-size: 12px; line-height: 1.5; overflow-x: auto; white-space: pre-wrap; word-break: break-all; max-height: 400px; overflow-y: auto;"><%== JSON.pretty_generate(result) %></pre>
  </div>
  <% end %>
</div>

<style>
  .info-item { display: flex; flex-direction: column; gap: 2px; }
  .info-label { color: #64748b; font-size: 12px; }
  .info-value { color: #e2e8f0; font-size: 14px; }
</style>

<script>
document.getElementById('toggle_raw_btn')?.addEventListener('click', function() {
  var block = document.getElementById('raw_data_block');
  if (block.style.display === 'none') {
    block.style.display = 'block';
    this.textContent = '收起';
  } else {
    block.style.display = 'none';
    this.textContent = '展开';
  }
});

document.addEventListener('click', function(e) {
  var btn = e.target.closest('.copy-link-btn');
  if (!btn) return;
  var url = btn.getAttribute('data-url') || '';
  var ta = document.createElement('textarea');
  ta.value = url;
  ta.style.position = 'fixed';
  ta.style.opacity = '0';
  document.body.appendChild(ta);
  ta.select();
  try {
    document.execCommand('copy');
    var orig = btn.textContent;
    btn.textContent = '已复制';
    setTimeout(function() { btn.textContent = orig; }, 1500);
  } catch(err) {}
  document.body.removeChild(ta);
});
</script>
